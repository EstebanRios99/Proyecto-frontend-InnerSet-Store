import React from 'react';
import withAuth from '../hocs/withAuth';

const Profile = () => {
  return <>
    <h1 className='title'>
      Perfil de Usuario 
    </h1>
  </>;
};

export default Profile;